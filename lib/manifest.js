// ============================================================================
// LIB MANIFEST — Loaded libraries tracking
// ============================================================================

window.__LIBS__ = {
  gsap: typeof gsap !== 'undefined',
  scrollTrigger: typeof ScrollTrigger !== 'undefined',
};

console.log('Libraries loaded:', window.__LIBS__);
